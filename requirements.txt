# فاز ۱ — لایهٔ داده
requests>=2.31,<3
python-dotenv>=1.0,<2
pandas>=2.2,<3

# فاز ۲ — اندیکاتورها (pandas-ta اصلی به‌خاطر نگرانی‌های امنیتی/تغییر مالکیت ریپو کنار گذاشته شد، از فورک جامعه‌محور استفاده می‌شه)
pandas-ta-classic>=0.6

# فاز ۸ — امنیت (رمزنگاری کلید API) و داشبورد وب سبک
cryptography>=42
flask>=3.0,<4
